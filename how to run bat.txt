From File Explorer:

1. Open `C:\Users\shaba\Downloads\git-clone\Strmlt_Zrdh`
2. Double-click [Run_Streamlit.bat](C:\Users\shaba\Downloads\git-clone\Strmlt_Zrdh\Run_Streamlit.bat)
3. Wait for the browser to open. If it doesn’t, visit `http://localhost:8501`.

Or from PowerShell:

```powershell
cd C:\Users\shaba\Downloads\git-clone\Strmlt_Zrdh
.\Run_Streamlit.bat
```

Keep the command window open while using the app. Press `Ctrl+C` in that window to stop it.

If it reports that `uv` is missing, install it with:

```powershell
winget install --id=astral-sh.uv
```