Implemented the programmatic Kite login flow.

Changed:
- [backend/app.py](c:/Users/shaba/Downloads/git-clone/portfolio_terminal/backend/app.py): added `/api/auth/kite/login` and `/api/auth/kite/callback`.
- [backend/portfolio_snapshot.py](c:/Users/shaba/Downloads/git-clone/portfolio_terminal/backend/portfolio_snapshot.py): exchanges `request_token` using `ZERODHA_API_SECRET`, stores daily session in `backend/.kite_session.json`, and uses that token for live holdings.
- [src/App.tsx](c:/Users/shaba/Downloads/git-clone/portfolio_terminal/src/App.tsx): shows a “Sign in with Kite” button when auth is missing.
- [.gitignore](c:/Users/shaba/Downloads/git-clone/portfolio_terminal/.gitignore): ignores `backend/.env` and `backend/.kite_session.json`.
- [backend/README.md](c:/Users/shaba/Downloads/git-clone/portfolio_terminal/backend/README.md): updated setup instructions.

You now need `backend/.env` to include:

```text
ZERODHA_API_KEY=...
ZERODHA_API_SECRET=...
```

Also set your Kite developer console redirect URL to:

```text
http://127.0.0.1:8000/api/auth/kite/callback
```

Then start backend and frontend. If the token is missing/expired, click “Sign in with Kite”; the backend will save the daily token automatically.

Verified:
- `uv run python -m py_compile backend\app.py backend\portfolio_snapshot.py`
- `npm.cmd run build` passed, with only the existing Vite chunk-size warning.

2026-07-09 note:
- Streamlit error reported: `Error fetching dashboard data: HTTPSConnectionPool(host='api.kite.trade', port=443): Read timed out. (read timeout=7)`.
- The error string comes from the Streamlit historical dashboard path in `getHoldings.py`, not the React/FastAPI backend.
- KiteConnect defaults to a 7 second request timeout when `timeout=None`.
- Lightest viable fix, if approved later: set a configurable Kite timeout once in `kite_auth.py` when creating `KiteConnect`, likely defaulting to 20 seconds.
- Lower-latency alternative: keep the 7 second timeout and only isolate per-symbol historical failures so one slow symbol does not fail the whole dashboard.
- More forgiving option: use a 20 second Kite timeout plus per-symbol skipping in `kite_analytics.py`.
- Optional mirror: apply the same timeout setting in `portfolio_terminal/backend/portfolio_snapshot.py` for the React/FastAPI path.
- No implementation was done for this timeout issue per user instruction.
