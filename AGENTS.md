do not make code changes before confirming first

Before implementing, call out the lightest viable logic and any lower-latency alternatives, especially when API calls, full refreshes, historical data loads, builds, or network work may grow. Prefer local patching, caching, deferred refresh, and explicit user-triggered full syncs when basic functionality allows it.
